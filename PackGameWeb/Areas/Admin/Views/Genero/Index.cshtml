@model IEnumerable<PackGameData.DataContext.Genero>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.generoGame)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.generoGame)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idGenero }) |
            @Html.ActionLink("Details", "Details", new { id=item.idGenero }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idGenero })
        </td>
    </tr>
}

</table>
